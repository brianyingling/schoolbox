.container
  %br
  %h4= "Student Roster for #{@klass.course.name}"
  %h5= "Period #{@klass.period}"
  %h5= "Current Class Average: #{@klass.total_average}" if @auth.is_a? Teacher
  %table
    = render :partial => 'klasses/klass_table_header',          :locals => {:klass => @klass}
    = render :partial => 'klasses/klass_table_student_roster',  :locals => {:klass => @klass}
